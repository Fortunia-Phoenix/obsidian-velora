# Velora 功能与注意事项

本文档记录 Velora 当前实际实现的功能、配置入口和已知边界。文档基于主题 `1.3.3`、仓库提交 `80d0c59` 和 2026-07-28 的 `theme.css` 核对结果。

## 0. 维护约定

- 本文档是后续修改 Velora 时的功能事实来源和验收依据，不是一次性的介绍文档。
- 修改前必须找出受影响的 `VEL-*` 编号和 Style Settings 表格行；修改后必须按最终实现同步更新功能、注意事项、默认值、范围、设备适配、兼容性、性能影响以及主题/Nexus 边界。
- 新功能必须增加稳定编号；删除、重命名或改变既有行为时必须更新原记录和 CHANGELOG，不能只改代码。
- `theme.css`、`manifest.json`、README、CHANGELOG 和本文档的相关修改必须进入同一个 Git 提交。
- 当前 Vault 主题目录中的本文档必须与 GitHub 仓库副本保持一致，并在推送后校验文件哈希、上游提交和工作区状态。
- 只有用户明确确认要改变某个具体功能时，才允许改变对应 `VEL-*` 功能契约。未获得确认时，修复、重构、性能优化、视觉调整、兼容性修改或其他任意改动，都必须保证所有受影响功能继续按本文档正常实现。
- 如果拟议实现不可避免会改变受保护功能，必须在编辑代码前列出受影响编号、当前行为、拟议行为、兼容性影响以及迁移或回退方案，等待用户确认。更新本文档本身不构成功能变更授权，也不能通过改写文档来合理化功能退化。

## 1. 能力边界

- Velora 是纯 CSS 主题。主题可以改变布局、颜色、尺寸、圆角、动画和已有控件的外观，不能创建真正的新按钮、命令或文件跳转功能。
- 主题只需要 `manifest.json` 和 `theme.css`，不安装其他插件也能正常显示；`manifest.json` 必须是 Obsidian 可直接解析的无 BOM UTF-8 JSON，否则 CSS 仍可能按文件夹名加载，但“外观 → 已安装主题”无法枚举该主题。
- Style Settings 是可选插件。未安装时使用主题默认值；安装后才能在图形界面中修改本文档列出的配置。
- Velora Nexus 不包含在主题仓库中，也不是主题正常渲染的必要条件。它只负责纯 CSS 无法可靠完成的行为增强。
- 主题保留原生下拉菜单的展开逻辑。关闭状态有圆角，展开列表由 Obsidian/操作系统负责，不做完整胶囊改造，以避免错位、透明叠字和跨平台差异。
- 笔记属性区域保留 Obsidian 原生控件样式。第三方控件的广泛美化会主动排除 `.metadata-container`。

## 2. 功能清单

### 2.1 全局外观

| 编号 | 功能 | 当前实现 | 注意事项 |
| --- | --- | --- | --- |
| VEL-001 | 浅色与深色模式 | 为 Obsidian 的浅色、深色模式分别定义背景、文本、边框、交互色和代码配色。 | 颜色跟随 Obsidian 当前明暗模式；不是主题内独立的明暗开关。 |
| VEL-002 | 六套全局配色 | Velora、Aurora、Pretty Note、Ocean、Mint、Minimal 会同时切换背景、面板、链接、标签、图谱、复选框和强调色。 | 需要 Style Settings 才能从界面切换；未安装时使用 Velora 默认配色。 |
| VEL-003 | 液态流光背景 | 使用固定的多层径向渐变和缓慢位移动画作为全局背景。 | 没有粒子效果，也没有单独的流动模式开关；“减少界面动画”或系统减少动态效果会停止动画。 |
| VEL-004 | 玻璃面板 | Markdown、空白页、侧边栏和 Bases 使用半透明面板、边框、阴影和模糊；不支持 `backdrop-filter` 时侧边栏回退到可读的实色面板。 | `backdrop-filter` 会增加 GPU 开销；低性能设备可降低模糊和不透明效果，能力回退不改变支持该效果的 Obsidian/Electron 外观。 |
| VEL-005 | 工作区分隔 | 主视图和侧边栏叶片具有可调间距、圆角和独立外框。 | 分屏较窄时会受响应式规则限制，实际间距可能小于桌面设置值。 |
| VEL-006 | 字体方案 | 提供现代无衬线、中文阅读、文章衬线三套字体栈。 | 主题不会下载字体；设备未安装首选字体时自动使用后备字体。 |
| VEL-007 | 界面密度 | 舒适与紧凑两档会调整标签栏、视图标题栏、文件树和 Bases 行高。 | 紧凑模式不会缩小所有第三方插件界面。 |

### 2.2 标签栏、按钮和原生控件

| 编号 | 功能 | 当前实现 | 注意事项 |
| --- | --- | --- | --- |
| VEL-010 | 独立胶囊标签 | 每个文档标签拥有完整圆角、描边、活动态和关闭按钮对齐；桌面右侧浮动栏使用固定的紧凑图标标签，避免默认 `300px` 宽度只露出前两个标签。 | 文档标签过多时标签容器滚动；右侧浮动栏超过可容纳数量时仍沿用原生横向滚动，最大宽度受 Style Settings 和设备断点共同限制。 |
| VEL-011 | 新建标签与标签列表 | 保留 Obsidian 原生“加号”和标签列表按钮，并放在标签序列后方；桌面右侧浮动栏会回收其垂直下移后不再需要的窗口控制区占位。 | 主题只改布局和外观，不改变按钮命令；常规顶栏继续保留窗口控制区安全距离，只有已位于窗口控制区下方的右侧浮动栏使用紧凑布局。 |
| VEL-012 | 视图操作按钮 | 标题栏右侧已有操作按钮统一为圆形按钮，并提供悬停和键盘焦点状态。 | 插件自行创建且不使用 Obsidian 标准类名的按钮可能不会被命中。 |
| VEL-013 | 表单控件 | 搜索框、文本框、文本域、按钮、复选框和切换开关采用统一圆角和配色；设置页按钮悬停时保持原位，避免与同一行的原生图标和下拉框错位。 | 下拉框展开部分保持原生；属性面板保持原生；第三方插件自定义位移不由主题接管。 |
| VEL-014 | 弹窗和命令面板 | Modal、Prompt、建议列表和 Obsidian 菜单使用玻璃表面、圆角和主题色选中态。 | 原生 HTML `select` 的系统菜单不属于 `.menu`，不会使用这套菜单外观。 |
| VEL-015 | 状态栏 | 状态栏显示为右下角胶囊，并可通过设置隐藏。 | 隐藏后状态信息仍由插件产生，只是不显示；主题不能重新安置其命令。 |
| VEL-016 | 滚动条 | 全局滚动条使用主题强调色和可调尺寸。 | 操作系统或 WebView 可能覆盖部分滚动条样式。 |

### 2.3 文件浏览器

| 编号 | 功能 | 当前实现 | 注意事项 |
| --- | --- | --- | --- |
| VEL-020 | 文件树条目 | 文件夹和文件具有圆角、悬停位移、活动文件背景和主题色。 | 超长文件名仍由 Obsidian 的截断规则处理。 |
| VEL-021 | 彩虹文件夹 | 顶级文件夹按顺序循环使用 7 种颜色，子文件夹继承所属顶级文件夹色系。 | 颜色由 `nth-child` 和当前排序决定；新增、移动或重排文件夹会改变后续颜色。 |
| VEL-022 | 彩虹强度调节 | 可分别调节顶级背景、顶级文字、悬停背景、子级背景和子级文字强度。 | 只作用于原生文件浏览器，不保证适配第三方文件树。 |

### 2.4 Markdown 笔记

| 编号 | 功能 | 当前实现 | 注意事项 |
| --- | --- | --- | --- |
| VEL-030 | 阅读与编辑排版 | 阅读模式和 Live Preview 共享正文字号、行高、标题层级和上下留白。 | “内容宽度”只限制阅读模式的预览容器；编辑模式继续使用当前窗格宽度。 |
| VEL-031 | Markdown 外框 | Markdown 窗格外围使用可调宽度和颜色强度的主题描边。 | 这是整个 Markdown 视图的外框，不是正文内部每个段落的边框。 |
| VEL-032 | 标题层级 | H1 使用渐变底线和图形标记，H2 使用卡片标题，H3 使用小圆点标记；编辑模式有对应样式。 | 手机端会限制 H1/H2/H3 的字号，防止标题溢出。 |
| VEL-033 | H2 短区块卡片 | 阅读模式将短 H2 区块的标题和内容连成圆角卡片。 | 纯主题无法数源文件行数，只能对最多约 5 个连续渲染块做回退判断；复杂嵌入、代码块或插件渲染可能不符合预期。 |
| VEL-034 | H2 长区块标题模式 | 被 Nexus 标记为长区块时，只保留 H2 标题卡片，下面内容不再包框。 | 真正的“120 行”判断必须单独安装并启用 Velora Nexus；主题仓库本身不执行行数计算。 |
| VEL-035 | 正文元素 | 美化引用、无序/有序列表、任务列表、表格、链接、高亮、分隔线、Callout 和粗体强调。 | 对齐设置主要作用于阅读模式；含代码的段落和引用在两端对齐模式下会回退为起始对齐。 |
| VEL-036 | 代码显示 | 为浅色和深色模式定义内联代码、代码块以及常见语法 token 配色。 | 实际语法 token 取决于 Obsidian 使用的高亮器和语言定义；未知 token 使用普通代码颜色。 |
| VEL-037 | Callout 分类色 | 信息、成功、警告、错误等 Callout 使用不同语义色。 | Pretty Note 配色会让 note/info/todo 更接近主题主色。 |
| VEL-038 | 正文与标题对齐 | 正文支持左对齐、两端对齐、居中；标题支持左对齐、居中。 | 编辑器正文固定左对齐，避免 CodeMirror 光标和字符位置异常；标题居中仍可作用于编辑模式标题。 |

### 2.5 背景图片

| 编号 | 功能 | 当前实现 | 注意事项 |
| --- | --- | --- | --- |
| VEL-040 | 独立明暗背景图 | 浅色与深色模式可以填写不同背景图，并分别随模式切换。 | 输入值必须是合法 CSS 图片值，例如 `url("https://example.com/image.jpg")`，不能填写 Markdown 图片语法或 `[[文件]]`。 |
| VEL-041 | 背景图效果 | 可调不透明度、模糊、饱和度和亮度，图片固定铺满视口。 | 远程图片依赖网络；本地绝对路径无法在其他电脑、iPad 或手机上直接复用。模糊会放大图片边缘并增加渲染开销。 |

### 2.6 侧边栏

| 编号 | 功能 | 当前实现 | 注意事项 |
| --- | --- | --- | --- |
| VEL-050 | 左侧浮动侧边栏 | 桌面端把左侧栏变为固定浮层，默认收在屏幕左侧，悬停把手或侧栏时展开。 | 仅匹配 `body:not(.is-mobile)`；Obsidian 标记为移动端的 iPad 和手机不会启用该布局。 |
| VEL-051 | 自动缩回 | 已启用的左、右浮动侧栏在鼠标离开后按同一设定延迟收回；重命名文件或侧栏菜单打开时保持展开。 | 这是 CSS 悬停逻辑，不是完整状态机；某些第三方弹窗、键盘操作或拖放状态可能无法阻止缩回。 |
| VEL-052 | 浮动侧栏把手 | 左侧栏在右边缘、右侧栏在左边缘使用镜像伪元素显示外置把手，展开后隐藏。 | 把手不是可聚焦按钮，没有命令，也不能点击切换；它的作用是提供悬停区域。 |
| VEL-053 | 侧栏尺寸与位置 | 左右浮动侧栏共享宽度和高度，并可分别调整展开后的左侧、右侧距离。 | 不支持直接拖拽；宽度还受 `220px` 最小值、`70vw/720px` 最大值限制，高度受 `320px` 和 `96vh` 限制。 |
| VEL-054 | 隐藏左侧功能区 | 隐藏桌面 Ribbon 和移动抽屉 Ribbon；触控设备额外隐藏左侧切换按钮。 | 只是视觉隐藏，不会禁用相关命令；隐藏后需通过命令面板、快捷键或设置恢复入口。 |
| VEL-055 | 侧边栏液态玻璃 | 对侧栏内容、底部 Vault 区域和浮动侧栏加入多层流光、模糊和统一圆角；重复的浅色流光定义由内部变量复用。 | 默认关闭；提高强度、模糊或缩短周期会增加 GPU 开销。手机窄屏会停用主要动画；内部变量不是 Style Settings 公共接口。 |
| VEL-056 | 减少侧栏动画 | 主题开关或系统 `prefers-reduced-motion` 会停止左右侧栏的液态玻璃动画；主题开关使用合并后的受限选择器覆盖所有液态层。 | 只停止动画，不会自动取消透明度和模糊；桌面与移动端的现有静态玻璃表面保持可用。 |
| VEL-057 | 右侧浮动侧边栏 | 通过独立 Style Settings 开关把桌面右侧栏变为固定浮层；默认收在屏幕右侧，从左边缘镜像把手悬停展开，并复用左侧栏的尺寸、表面、液态玻璃和缩回设置；展开后的标签栏使用紧凑图标命中区，在默认宽度下完整显示当前六个侧栏标签及原生“加号”和标签列表按钮。 | 仅匹配 `body:not(.is-mobile)`；与左侧开关相互独立，同时启用时两侧都会覆盖主工作区边缘；增加更多侧栏标签后仍可能触发原生横向滚动。 |

### 2.7 Obsidian Bases

| 编号 | 功能 | 当前实现 | 注意事项 |
| --- | --- | --- | --- |
| VEL-060 | Bases 主视图 | 美化独立 Bases 视图、Markdown 内嵌 Base 和代码块 Base 的主题变量。 | Bases 必须由当前 Obsidian 版本提供；主题的最低版本声明不代表旧版 Obsidian 拥有 Bases。 |
| VEL-061 | Bases 表格 | 调整表头、斑马行、悬停、活动单元格、汇总行、列按钮和多选标签。 | 大表格仍由 Obsidian 自身虚拟化和滚动；主题不改变数据行为。 |
| VEL-062 | Bases 卡片与列表 | 卡片具有圆角、封面、阴影和桌面悬停位移，列表和分组具有主题选中态。 | 触控设备不依赖悬停；第三方修改 Bases DOM 后可能需要单独适配。 |
| VEL-063 | Bases 工具栏与筛选器 | 工具栏、搜索框、菜单、筛选表达式、排序和分组编辑器采用 Velora 外观。 | Bases 内部类名属于 Obsidian 实现细节，Obsidian 大版本更新后需要复查。 |

### 2.8 兼容性、响应式和无障碍

| 编号 | 功能 | 当前实现 | 注意事项 |
| --- | --- | --- | --- |
| VEL-070 | 插件兼容优先 | 默认只广泛美化 Obsidian 原生控件，减少覆盖第三方插件。 | 插件仍会继承主题颜色变量，外观不一定完全一致。 |
| VEL-071 | 插件统一美化 | 可选择广泛覆盖第三方文本框、下拉框、文本域、按钮和复选框。 | 可能与插件自己的尺寸、定位和交互 CSS 冲突；出现异常时切回“兼容优先”。 |
| VEL-072 | 属性面板保护 | 统一美化选择器排除 `.metadata-container`，保留原生属性体验。 | 插件伪装成属性区域但不使用该类名时，不一定受保护。 |
| VEL-073 | iPad/平板适配 | 在 721-1100px 或粗指针平板上收紧间距、圆角、标签、标题栏、笔记内边距和弹窗；当 Obsidian 移动导航栏已提供左侧抽屉入口时，隐藏视图标题栏中重复的左侧栏按钮。 | 浮动侧栏明确禁用于 `.is-mobile`；顶部原生左侧栏入口仍保留；液态玻璃静态/动画行为取决于设备宽度和减少动态效果设置。 |
| VEL-074 | 手机适配 | 720px 以下缩小标签和面板，处理安全区，限制标题字号，并让表格、代码、公式和媒体可横向滚动或自适应。 | 手机布局优先避免溢出，不保证与桌面视觉尺寸完全一致。 |
| VEL-075 | 键盘焦点 | 保留 `:focus-visible` 并允许调整焦点环宽度。 | 不建议把宽度设得过小；主题不会为第三方非标准控件补充缺失的键盘语义。 |
| VEL-076 | 减少动态效果 | 可关闭主题动画，并尊重系统 `prefers-reduced-motion`。 | 部分简单颜色变化仍可能保留；该设置不等同于低性能模式。 |

## 3. Style Settings 完整参考

### 3.1 外观与界面

| 设置 | 默认值 | 范围/选项 | 注意事项 |
| --- | --- | --- | --- |
| 全局配色方案 | Velora | Velora / Aurora / Pretty Note / Ocean / Mint / Minimal | 切换整套颜色 token，不只改变强调色。 |
| 字体方案 | 现代无衬线 | 现代无衬线 / 中文阅读 / 文章衬线 | 依赖设备已安装字体。 |
| 界面密度 | 舒适 | 舒适 / 紧凑 | 主要影响原生界面和 Bases。 |
| 主面板不透明度 | `0.78` | `0.4-1` | 越低越能看到背景，也越可能降低文字对比度。 |
| 玻璃模糊强度 | `22px` | `0-40px` | 高值增加 GPU 开销。 |
| 主面板圆角 | `28px` | `8-40px` | 平板和手机会设置更小上限。 |
| 工作区间距 | `12px` | `4-24px` | 窄屏会自动收紧。 |

### 3.2 标签栏与按钮

| 设置 | 默认值 | 范围 | 注意事项 |
| --- | --- | --- | --- |
| 胶囊标签高度 | `30px` | `26-40px` | 手机会使用更紧凑的默认变量。 |
| 标签最大宽度 | `210px` | `120-360px` | 平板约限制到 `190px`，手机约限制到 `154px`。 |
| 标签描边强度 | `0.48` | `0.1-1` | 过高会让每个标签边框很重。 |
| 视图操作按钮尺寸 | `32px` | `26-40px` | 平板/手机有独立触控尺寸。 |

### 3.3 彩虹文件夹

| 设置 | 默认值 | 范围 | 注意事项 |
| --- | --- | --- | --- |
| 启用彩虹文件夹 | 关闭 | 开/关 | 只作用于原生文件浏览器。 |
| 顶级文件夹背景不透明度 | `0.10` | `0-1` | 高值可能影响文字对比度。 |
| 顶级文件夹文字色彩强度 | `0.68` | `0-1` | 越高越接近彩虹色。 |
| 文件夹悬停背景不透明度 | `0.16` | `0-1` | 同时作用于顶级和子级悬停。 |
| 子文件夹背景不透明度 | `0.06` | `0-1` | 子级继承顶级色系。 |
| 子文件夹文字色彩强度 | `0.58` | `0-1` | 过高时深浅模式可能需要分别检查。 |

### 3.4 笔记排版

| 设置 | 默认值 | 范围/选项 | 注意事项 |
| --- | --- | --- | --- |
| 内容宽度 | `1080px` | `600-1600px` | 只限制阅读模式内容宽度。 |
| 内容上下留白 | `38px` | `12-80px` | 手机使用单独的安全区内边距。 |
| 正文字号 | `16px` | `13-24px` | 手机最大显示约 `16px`。 |
| 一级标题字号 | `36px` | `26-56px` | 手机限制约 `30-34px`。 |
| 二级标题字号 | `25px` | `18-40px` | 手机限制约 `23-27px`。 |
| 三级标题字号 | `21px` | `16-32px` | 手机最大约 `18px`。 |
| 正文行高 | `1.78` | `1.4-2.2` | 手机最大约 `1.74`。 |
| 笔记卡片圆角 | `16px` | `0-32px` | 影响 H2 区块、表格、代码块和 Callout 的派生圆角。 |
| 笔记卡片透明度 | `0.68` | `0.3-1` | 影响 H2 区块等内部表面，不等同于整个 Markdown 外框透明度。 |
| Markdown 外框宽度 | `1px` | `1-4px` | 调整整个 Markdown 窗格边框。 |
| Markdown 外框颜色强度 | `0.46` | `0.1-0.9` | 与当前配色强调色联动。 |
| 正文对齐 | 左对齐 | 左对齐 / 两端对齐 / 居中 | 主要作用于阅读模式。 |
| 标题对齐 | 左对齐 | 左对齐 / 居中 | 居中时 H2 左侧圆点会隐藏。 |

### 3.5 背景图片

| 设置 | 默认值 | 范围 | 注意事项 |
| --- | --- | --- | --- |
| 启用背景图片 | 关闭 | 开/关 | 开启前先填写至少一个模式的合法 CSS 图片值。 |
| 浅色背景图片 URL | `none` | CSS 文本 | 使用 `url("...")`。 |
| 深色背景图片 URL | `none` | CSS 文本 | 使用 `url("...")`。 |
| 背景图片不透明度 | `0.18` | `0-0.8` | 高值可能降低正文对比度。 |
| 背景图片模糊 | `0px` | `0-20px` | 会增加合成开销。 |
| 背景图片饱和度 | `1` | `0-3` | `1` 为原始饱和度。 |
| 背景图片亮度 | `1` | `0-3` | `1` 为原始亮度。 |

### 3.6 悬浮侧边栏

| 设置 | 默认值 | 范围 | 注意事项 |
| --- | --- | --- | --- |
| 启用左侧浮动侧边栏 | 关闭 | 开/关 | 仅桌面端，且会隐藏原生左侧栏切换按钮。 |
| 隐藏左侧功能区 | 关闭 | 开/关 | 隐藏 Ribbon，不删除命令。 |
| 浮动侧边栏宽度 | `300px` | `220-480px` | 还受视口最大宽度限制，不支持拖拽。 |
| 浮动侧边栏高度 | `86%` | `50-96%` | 还受 `320px` 最小高度限制。 |
| 展开后左侧距离 | `14px` | `0-48px` | 只影响展开位置。 |
| 浮动侧边栏透明度 | `0.88` | `0.35-1` | 过低会影响文字对比度。 |
| 侧边栏玻璃模糊 | `22px` | `0-40px` | 高值增加 GPU 开销。 |
| 侧边栏圆角 | `22px` | `8-36px` | 同时影响顶部和底部区域衔接。 |
| 启用液态玻璃效果 | 关闭 | 开/关 | 默认关闭；无粒子效果。 |
| 液态玻璃流光强度 | `0.16` | `0.08-0.6` | 高值更显眼，也更容易影响可读性。 |
| 液态玻璃流动周期 | `18s` | `10-40s` | 数值越小动画越快。 |
| 自动缩回延迟 | `1.2s` | `0-10s` | 仅控制鼠标离开后的 CSS 过渡延迟。 |
| 把手宽度 | `14px` | `6-28px` | 把手是悬停区域，不是真正按钮。 |
| 把手高度 | `56px` | `28-120px` | 过小会难以触发，过大会更显眼。 |

### 3.7 兼容性与辅助

| 设置 | 默认值 | 范围/选项 | 注意事项 |
| --- | --- | --- | --- |
| 第三方插件界面 | 兼容优先 | 兼容优先 / 统一美化 | 统一美化出现布局问题时切回兼容优先。 |
| 减少界面动画 | 关闭 | 开/关 | 停止主题动画，不取消模糊。 |
| 隐藏底部状态栏 | 关闭 | 开/关 | 只改变显示。 |
| 键盘焦点环宽度 | `2px` | `1-4px` | 保留清晰焦点有助于键盘操作。 |
| 滚动条尺寸 | `12px` | `8-18px` | 系统可能覆盖样式。 |

## 4. Velora Nexus 的可选能力

Velora Nexus 当前公开实现只有 H2 区块精确行数分类，不包含以下尚未实现或已移除的能力：Notion 式导航、收藏、最近页面、快捷入口、侧栏拖拽改宽高。

正确安装并启用 Nexus 后，它会：

1. 读取 Markdown 源文件并识别 H1/H2 边界，忽略围栏代码块内看似标题的文本。
2. 从 H2 标题行统计到下一个 H1/H2 前一行；最后一个 H2 统计到最后一行非空内容。
3. 使用主题变量 `--velora-section-card-line-limit`，当前默认 `120`，允许范围 `20-1000`。
4. 为短区块添加 `velora-h2-card-block` 等类，为长区块添加 `velora-h2-long-block`，由主题负责最终外观。
5. 在文件修改、删除或重命名时清理缓存，并在插件卸载时移除生成类。

注意：

- Nexus 必须位于 `.obsidian/plugins/velora-nexus/`，并让 `manifest.json` 直接位于该目录，不能再嵌套一层文件夹。
- Nexus 必须在“第三方插件”中启用；仅把文件放进插件目录不会运行。
- Nexus 不负责主题配色、侧栏尺寸或液态玻璃；这些仍由 `theme.css` 和 Style Settings 管理。
- 不安装 Nexus 时 Markdown 仍可正常渲染，只是 H2 短区块使用 CSS 粗略回退判断。

## 5. 维护检查表

以后修改 Velora 时，应同步检查：

1. 新功能是否属于纯 CSS 能力，还是需要插件行为。
2. 是否为功能增加或更新 Style Settings 元数据。
3. 浅色、深色和六套配色是否都正常。
4. 桌面、iPad/平板、手机和窄分屏是否无溢出。
5. 属性面板和原生下拉菜单是否仍保持稳定。
6. “兼容优先”是否没有广泛覆盖第三方插件。
7. 动画是否尊重主题和系统的减少动态效果设置。
8. README、本文档和 CHANGELOG 是否与 `theme.css` 一致。
9. 本地安装副本与 GitHub 文档的哈希、提交和上游状态是否已经核对。
10. 未经用户确认的功能是否全部完成了针对受影响 `VEL-*` 编号的回归验证。
